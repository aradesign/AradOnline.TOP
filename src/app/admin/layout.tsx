export const metadata = {
  title: "پنل مدیریت | آراد وب",
  robots: "noindex, nofollow",
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
